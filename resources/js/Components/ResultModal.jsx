import React from 'react'
import ModalComponent from './ModalComponent'

const ResultModal = ({show,handleClose}) => {
  return (
    <ModalComponent show={show} handleClose={handleClose}>
        <div>
    
        </div>
    </ModalComponent>
  )
}

export default ResultModal
